 try
                    {
                        Directory.CreateDirectory(directory);
                    }
                    catch (Exception ex)
                    {
                        //MessageBox.Show(Das System konnte den geforderten Pfad nicht erzeugen.nrFehler: nr + ex.ToString());
                    }
